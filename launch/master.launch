<launch>
  <!-- teleop -->
  <include file="$(find solace)/launch/teleop.launch" />

  <!-- navigation stack -->
  <include file="$(find solace)/launch/move_base.launch" />
  <node name="navigation_twist_to_vesc" type="navigation_twist_to_vesc.py" pkg="solace" />

  <!-- gmapping map saver -->
  <node name="map_saver" type="map_saver" pkg="map_server" args="-f gmapping_map">
    <remap from="map" to="gmapping_disk_map"/>
  </node>

  <!-- full map saver -->
  <node name="map_saver" type="map_saver" pkg="map_server" args="-f full_map">
    <remap from="map" to="full_disk_map"/>
  </node>

  <!-- navigation map server -->
  <node name="navigation_map_server" type="navigation_map_server.py" pkg="solace" />

  <!-- lost detector -->
  <node name="lost_detector" type="lost_detector.py" pkg="solace" output="screen" />

  <!-- mapping controller -->
  <node name="mapping_controller" type="mapping_controller.py" pkg="solace" output="screen" />
</launch>
